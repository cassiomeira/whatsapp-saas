import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.crmmobilewrapper',
  appName: 'crm-mobile-wrapper',
  webDir: 'dist'
};

export default config;
